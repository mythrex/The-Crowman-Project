(function() {
  'use strict';
  var Bluebird, Sequelize;

  Bluebird = require('bluebird');

  Sequelize = require('sequelize');

  module.exports = {
    up: function(db) {
      return Bluebird.delay(1000).then(function() {
        return db.bulkInsert('Person', [
          {
            name: 'John Doe'
          }
        ], {
          name: {}
        });
      }).then(function() {
        return db.bulkInsert('Task', [
          {
            title: 'Find own identity'
          }
        ], {
          title: {}
        });
      });
    }
  };

}).call(this);
