(function() {
  var nodeify;

  nodeify = require('nodeify');

  module.exports = {
    up: function(migration, DataTypes, done) {
      return nodeify(migration.bulkInsert('Person', [
        {
          name: 'John Doe',
          isBetaMember: false
        }
      ], {
        name: {},
        isBetaMember: {}
      }), done);
    },
    down: function(migration, DataTypes, done) {
      return nodeify(migration).bulkDelete('Person', null, {}, done);
    }
  };

}).call(this);
